# Example fine-tune dataset

Tiny subset of the [Vehicle](https://universe.roboflow.com/marchel-maulana-fahrezi/vehicle-emspn) Roboflow dataset (YOLOv8 format).

Use only to verify the training pipeline. For real results, download the full dataset from Roboflow Universe and point `finetune.py` at that `data.yaml`.
